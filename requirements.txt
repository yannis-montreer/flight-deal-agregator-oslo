httpx>=0.27,<1.0
PyYAML>=6.0,<7.0
python-dotenv>=1.0,<2.0
# Fournit les donnees IANA pour zoneinfo (stdlib) : python:3.12-slim (Docker) et Windows
# n'incluent pas la base de fuseaux horaires du systeme, contrairement a la plupart des
# distributions Linux completes. Recommandation officielle de la doc Python pour ce cas.
tzdata>=2024.1
